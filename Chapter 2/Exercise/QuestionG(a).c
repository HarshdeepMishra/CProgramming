/*
If a five digt number is input through the keyboard, write a program to calculate the sum of its digits and display the result on the screen.
*/
#include <stdio.h>
int main(){
    int number, sum = 0, digit;
    printf("Enter a five-digit number: ");
    scanf("%d", &number);
    
}